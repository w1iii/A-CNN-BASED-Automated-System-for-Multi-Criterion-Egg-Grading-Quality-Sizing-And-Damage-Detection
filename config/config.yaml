# config.yaml - example configuration for training and inference

data:
  raw_dir: "data/raw/"
  processed_dir: "data/Eggs Classification/"
  img_size: 224
  batch_size: 32
  num_workers: 4

model:
  type: "cnn"
  num_classes: 2  # Damaged, Not Damaged in this dataset
  pretrained: false

training:
  epochs: 20
  lr: 0.001
